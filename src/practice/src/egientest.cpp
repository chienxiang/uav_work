#include <iostream>
#include <Eigen/Dense>
#include <math.h>
using namespace std;
using namespace Eigen;
int main()
{
  // float x=1,y=2,z=3;
  // Vector3f p;   
  // Vector3f m(3,2,1);
  // p <<x,y,z; 
  // MatrixXf v = MatrixXf::Zero(3,2);

  // v << p,m;
  // // float x = (p+m).dot(p-m)/p.norm()*m.norm();
  // // v(0,0) = x;
  // cout << v(1,0)<<endl;
    
} 